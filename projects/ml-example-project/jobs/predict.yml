resources:
  jobs:
    ml-example-project-predict:
      name: ml-example-project-predict-${bundle.target}
      permissions:
        - group_name: data-scientists
          level: CAN_MANAGE_RUN

      job_clusters:
        - job_cluster_key: ml-example-project-predict
          new_cluster:
            num_workers: 1
            spark_version: 15.4.x-scala2.12
            node_type_id: Standard_DS4_v2
            custom_tags:
              user: "Data scientists"
              tool: "ML Example Project"
              env: ${bundle.target}
              managed_by: "manually"
            docker_image:
              url: "${var.docker_image_url}"
              basic_auth:
                username: "{{secrets/auth_common/docker-registry-username}}"
                password: "{{secrets/auth_common/docker-registry-password}}"

      email_notifications:
        on_failure:
          - sylvia.liu@cheffelo.com

      tasks:
        - task_key: ml-example-project-predict-company
          job_cluster_key: ml-example-project-predict
          notebook_task:
            notebook_path: ./predict.py
            base_parameters:
              env: "${var.environment}"
              company: AMK
              is_run_on_databricks: True
              predict_start_yyyyww: 202450
              predict_end_yyyyww: 202451
        - task_key: slack-notification-success
          depends_on:
            - task_key: ml-example-project-predict-company
          run_if: ALL_SUCCESS
          run_job_task:
            job_id: ${var.slack_notification_job_id} #Reads from the variable defined in the databricks.yml file
            job_parameters:
              environment: ${bundle.target} #Reads from the target environment as defined in the databricks.yml file
              body_message: ✅ ML Example project predict finished!
              is_error: false
        - task_key: slack-notification-failied
          depends_on:
            - task_key: ml-example-project-predict-company
          run_if: AT_LEAST_ONE_FAILED
          run_job_task:
            job_id: ${var.slack_notification_job_id} #Reads from the variable defined in the databricks.yml file
            job_parameters:
              environment: ${bundle.target} #Reads from the target environment as defined in the databricks.yml file
              message_header: ❌ ML Example project predict step failed!
              body_message: Check databricks for details...
              is_error: true
              relevant_people: sylvia
