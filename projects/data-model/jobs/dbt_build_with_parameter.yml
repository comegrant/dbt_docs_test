resources:
  jobs:
    dbt_build_with_parameter:
      name: dbt_build_with_parameter

      permissions:
        - group_name: data-engineers
          level: CAN_MANAGE_RUN
        - group_name: data-scientists
          level: CAN_MANAGE_RUN
        - group_name: data-analysts
          level: CAN_MANAGE_RUN

      environments:
        - environment_key: dbt-default
          spec:
            client: "1"
            dependencies:
              - dbt-databricks${var.dbt_databricks_version}

      tasks: 
        - task_key: dbt
          dbt_task:
            project_directory: ../transform/
            warehouse_id: ${var.warehouse_id}
            catalog: ${bundle.target}
            commands:
              - 'dbt deps'
              - 'dbt build {{job.parameters.dbt_extra_command}}'
          environment_key: dbt-default
          disable_auto_optimization: true
      parameters:
        - name: dbt_extra_command
          default:  -s +dim_recipes
      
      edit_mode: EDITABLE

      