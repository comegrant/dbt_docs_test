resources:
  jobs:
    delete_old_dbt_schema_tables:
      name: delete_old_dbt_schema_tables

      environments:
        - environment_key: dbt-default
          spec:
            client: "1"
            dependencies:
              - dbt-databricks>=1.0.0,<2.0.0

      tasks: 

        - task_key: delete_tables
          notebook_task: 
            notebook_path: ../useful/dbt_development/temp_bulk_delete_all_tables.py

      parameters:
        - name: firstname_lastname
          default: ""

      edit_mode: EDITABLE