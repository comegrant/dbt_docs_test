resources:
  jobs:
    dbt_developer_bulk_delete_tables:
      name: dbt_developer_bulk_delete_tables 

      environments:
        - environment_key: dbt-default
          spec:
            client: "1"
            dependencies:
              - dbt-databricks>=1.0.0,<2.0.0

      tasks: 

        - task_key: delete_tables_per_schema
          notebook_task: 
            notebook_path: ../useful/dbt_development/bulk_delete_tables.py

      parameters:
        - name: firstname_lastname
          default: ""

        - name: layer
          default: ""

      edit_mode: EDITABLE