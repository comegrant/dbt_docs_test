resources:
  jobs:
    developer_schema_bulk_empty_schema:
      name: developer_schema_bulk_empty_schema

      environments:
        - environment_key: dbt-default
          spec:
            client: "1"
            dependencies:
              - dbt-databricks>=1.0.0,<2.0.0

      tasks: 

        - task_key: delete_tables_per_schema
          notebook_task: 
            notebook_path: ../useful/dbt_development/bulk_empty_schema.py

      parameters:
        - name: firstname_lastname
          default: ""

        - name: layer
          default: ""

      edit_mode: EDITABLE