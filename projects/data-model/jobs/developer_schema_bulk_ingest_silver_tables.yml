resources:
  jobs:
    developer_schema_bulk_ingest_silver_tables:
      name: developer_schema_bulk_ingest_silver_tables 

      environments:
        - environment_key: dbt-default
          spec:
            client: "1"
            dependencies:
              - dbt-databricks==1.10.1

      tasks: 

        - task_key: ingest_silver_tables
          notebook_task: 
            notebook_path: ../useful/dbt_development/bulk_ingest_silver_tables.py

      parameters:
        - name: firstname_lastname
          default: ""

        - name: table_list
          default: ""

      edit_mode: EDITABLE