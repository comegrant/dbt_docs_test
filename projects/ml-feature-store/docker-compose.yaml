version: '3'
services:

  # This service mainly serves as a build step. Therefore, making sure the Dockerfile is build
  # It will also be used when starting a shell
  base:
    image: ml-feature-store-base
    platform: linux/amd64
    build:
      context: ../../
      dockerfile: projects/ml-feature-store/docker/Dockerfile
    command: ["echo", "'Hello from ml-feature-store'"]
    volumes:
      - ./:/opt/projects/ml-feature-store
      - ./../../packages:/opt/packages

  test:
    image: ml-feature-store-test
    platform: linux/amd64
    build:
      context: ../../
      dockerfile: projects/ml-feature-store/docker/Dockerfile.testing
    command: ["python", "-m", "pytest", "-rav", "tests"]
    volumes:
      - ./:/opt/projects/ml-feature-store
      - ./../../packages:/opt/packages
    depends_on:
      - base
    profiles: ["test"]
