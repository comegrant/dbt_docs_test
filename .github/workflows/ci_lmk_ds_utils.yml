---
name: CI packages/lmk-ds-utils

on:
  pull_request:
    paths:
      - '.github/workflows/python_reusable.yml'
      - '.github/workflows/ci_lmk_ds_utils.yml'
      - 'packages/lmk-ds-utils/**'
  workflow_dispatch:  # Allows to trigger the workflow manually in GitHub UI

jobs:
  ci-libs-lmk-ds-utils:
    uses:
      ./.github/workflows/python_reusable.yml
    with:
      working-directory: packages/lmk-ds-utils
    secrets: inherit
