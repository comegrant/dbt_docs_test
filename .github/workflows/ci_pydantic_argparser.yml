---
name: CI packages/pydantic-argparser

on:
  pull_request:
    paths:
      - '.github/workflows/python_reusable.yml'
      - '.github/workflows/ci_pydantic_argparser.yml'
      - 'packages/pydantic-argparser/**'
  workflow_dispatch:  # Allows to trigger the workflow manually in GitHub UI

jobs:
  ci-libs-pydantic-argparser:
    uses:
      ./.github/workflows/python_reusable.yml
    with:
      working-directory: packages/pydantic-argparser
    secrets: inherit
