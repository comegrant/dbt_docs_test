---
name: CI projects/data-catalog

on:
  pull_request:
    paths:
      - '.github/workflows/python_reusable.yml'
      - '.github/workflows/ci_data_catalog.yml'
      - 'projects/data-catalog**'
  workflow_dispatch:  # Allows to trigger the workflow manually in GitHub UI

jobs:
  ci-libs-data-catalog:
    uses:
      ./.github/workflows/python_reusable.yml
    with:
      working-directory: projects/data-catalog
    secrets: inherit